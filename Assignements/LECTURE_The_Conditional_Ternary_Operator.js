const population = 31000000;
const country = 'Portugal'
population > 3300000 ? console.log("Portugal's population is above average") :
    console.log("Portugal's population is below average")

console.log(`${country}'s population is ${population > 3300000 ? 'above' : 'below'} average`);


const age = 12;
console.log(`you are ${age >= 18 ? 'eligible' : 'not eligible'} to drive`);
